# Environment Variables Example
# Copy this file to .env and fill in your actual values

MONGODB_URI=mongodb+srv://your_username:your_password@cluster0.example.mongodb.net/linkedin_clone?retryWrites=true&w=majority
JWT_SECRET=your_super_secret_jwt_key_here_make_it_long_and_random
NODE_ENV=development

# Instructions:
# 1. Replace 'your_username' and 'your_password' with your actual MongoDB Atlas credentials
# 2. Replace 'cluster0.example' with your actual cluster address
# 3. Make sure the password doesn't contain special characters that need URL encoding
# 4. If your password has special characters, URL encode them:
#    @ = %40, # = %23, $ = %24, % = %25, etc.

# JWT Secret should be a long, random string (minimum 32 characters)
# You can generate one using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# Example MongoDB Atlas Connection String:
# MONGODB_URI=mongodb+srv://myuser:mypassword@cluster0.abc123.mongodb.net/linkedin_clone?retryWrites=true&w=majority

# SECURITY NOTE: Never commit the actual .env file to version control!
